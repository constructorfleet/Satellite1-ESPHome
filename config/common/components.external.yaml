substitutions:
  ext_comp_repo_url: https://github.com/constructorfleet/Satellite1-ESPHome
  ext_comp_repo_ref: develop

external_components:
  - source:
      type: git
      url: ${ext_comp_repo_url}
      ref: ${ext_comp_repo_ref}
    refresh: 1s
    components:
      - fusb302b
      - http_request
      - i2s_audio
      - memory_flasher
      - micro_wake_word
      - pcm5122
      - satellite1
      - satellite1_radar
      - tas2780
      - wake_audio_stream
